import { Link } from 'react-router-dom';
import { FaStar } from 'react-icons/fa';

const imageUrl = import.meta.env.VITE_IMG;

const MovieCard = (movie, s) => {
  return (
    <div>MovieCard</div>
  );
};

export default MovieCard;